# PostgreSQL connection
DATABASE_URL=postgresql://workshop:workshop@localhost:5432/bookstore

# Valkey server connection settings
VALKEY_HOST=localhost
VALKEY_PORT=6379

# Cache configuration
CACHE_TTL_SECONDS=300
CACHE_ENABLED=false
