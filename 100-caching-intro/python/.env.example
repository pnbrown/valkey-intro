# Artificial delay (in seconds) applied by the Data Source to simulate a slow external API
SLOW_DELAY_SECONDS=2.5

# Valkey server connection settings
VALKEY_HOST=localhost
VALKEY_PORT=6379

# Cache configuration
CACHE_TTL_SECONDS=30
CACHE_ENABLED=false
